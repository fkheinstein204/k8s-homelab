# Default values : https://github.com/bitnami-labs/sealed-secrets/blob/helm-v2.7.3/helm/sealed-secrets/values.yaml
sealed-secrets:
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
  metrics:
    serviceMonitor:
      enabled: false